import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    grid_buttons: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-evenly',
        marginTop: 50
    }
});